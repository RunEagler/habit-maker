export enum Priority {
  S = 1,
  A,
  B,
  C,
  D,
}

export enum Emergency {
  High = 1,
  Normal,
  Low,
}

export enum IssueStatus {
  Unsolved = 1,
  InProgress,
  Done,
}

export enum TaskStatus {
  NotCompleted,
  InProgress,
  Completed,
}
